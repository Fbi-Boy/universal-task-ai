# Approval Notifications

Notifications identify an existing approval request. The notification itself is never authorization; approval must pass through the approval-resume gateway and actor validation.
