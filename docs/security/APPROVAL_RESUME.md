# Approval Resume Gateway

Channel adapters may deliver an approval decision, but the decision must carry authenticated actor identity, run identity, and approval identity into the existing approval subsystem. A channel message alone never grants permission.