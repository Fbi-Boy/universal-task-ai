# WhatsApp Inbound Boundary

Inbound content is authenticated before parsing. Provider-specific payload mapping stays behind this boundary so untrusted message text cannot become a permission grant or bypass the universal task pipeline.