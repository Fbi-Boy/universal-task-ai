# Runtime Approval Wiring

Approval requests and approval decisions remain separate operations. The notification service cannot approve an action; only the injected resume gateway handles an explicit approval request.
