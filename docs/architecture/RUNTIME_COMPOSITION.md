# Runtime Composition

Runtime dependencies are assembled explicitly instead of being discovered from task input. This keeps privileged capabilities behind known application boundaries and makes integration tests deterministic.
