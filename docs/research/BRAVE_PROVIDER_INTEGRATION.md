# Brave Research Integration

The research layer consumes the existing Brave provider through an injected contract and requires an EvidenceBundle. Query and result bounds are enforced before provider access, and evidence is validated before returning it to orchestration.
