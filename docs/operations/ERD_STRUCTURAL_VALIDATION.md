# ERD Structural Validation

Checks explicit entity/table declarations, duplicate names, and PK/FK markers. It is deterministic and advisory until domain-specific relationship/cardinality checks are added.