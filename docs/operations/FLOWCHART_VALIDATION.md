# Flowchart Validation

The first deterministic gate checks that a generated flowchart has start/end markers and at least one transition. Future checks will validate reachable nodes, decision branches, dead ends, and structural consistency before delivery or browser submission.
