# Schedule Task Pipeline

Scheduled work enters the same task dispatcher as interactive work. The scheduler adds no execution privileges. Permissions, approval, audit, reviewer, retry, and finalization remain downstream.