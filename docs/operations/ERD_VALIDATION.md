# ERD Validation

The deterministic ERD validator is a first safety/quality gate. It checks for non-empty output and basic entity/table/key markers. Domain checks will expand to relationship consistency, cardinality, duplicate keys, and normalization heuristics before production artifact delivery.
